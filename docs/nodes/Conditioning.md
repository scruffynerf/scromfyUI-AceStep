# Conditioning Nodes

The Conditioning category is the heart of the "Scromfy Advantage". It empowers users to intercept, deconstruct, modify, and recombine the raw mathematical tensors generated by the text encoder before they hit the DiT sampler. This allows for complex audio algebra, explicit structural overrides, and masking logic that native ComfyUI fundamentally hides.

---

## Part 1: Deconstruction & Recombination

### 1. AceStepConditioningSplitter

*File: `nodes/conditioning_split_node.py`*

Deconstructs a full ACE-Step conditioning object into its isolated, mutable components.

- **Inputs**: `conditioning` (`CONDITIONING`)
- **Outputs**:
  - `timbre_tensor` (`TENSOR`)
  - `pooled_output` (`TENSOR`)
  - `lyrics_tensor` (`TENSOR`)
  - `audio_codes` (`LIST`)

### 2. AceStepConditioningCombine

*File: `nodes/conditioning_combine_node.py`*

Assembles separate raw tensors and lists back into a single legal ComfyUI conditioning object. Missing inputs are automatically padded/backfilled.

- **Inputs**: `timbre_tensor`, `pooled_output`, `lyrics_tensor`, `audio_codes` (All Optional).
- **Options**: `empty_mode` (`zeros`, `ones`, `random`), `seed`.
- **Outputs**: `CONDITIONING`.

### 3. AceStepConditioningMixer

*File: `nodes/conditioning_dual_mixer_node.py`*

Selectively routes internal components from two different conditioning inputs. (e.g., Take the `lyrics_tensor` from Song A, but the `audio_codes` from Song B).

- **Inputs**: `conditioning_A`, `conditioning_B`.
- **Options**: Dropdowns to select `A`, `B`, or `none` for `timbre_tensor_source`, `pooled_output_source`, `lyrics_source`, and `audio_codes_source`.
- **Outputs**: `CONDITIONING`.

### 4. AceStepConditioningZeroOut

*File: `nodes/conditioning_zero_out_node.py`*

Zeroes out all internal tensors of a conditioning object. Exclusively used for passing explicit negative/unconditional input to the sampler.

- **Inputs**: `conditioning` (`CONDITIONING`).
- **Outputs**: `zeroed_conditioning` (`CONDITIONING`).

---

## Part 2: 5Hz Audio Code Math (Discrete Tokens)

### 5. AceStepAudioCodesMixer

*File: `nodes/audio_codes_mixer_node.py`*

A robust binary toolbox for mixing two sets of list-based audio codes in raw 6D FSQ space. It temporarily decodes token IDs, performs continuous math, and re-quantizes them back to valid IDs.

- **Inputs**: `audio_codes_A`, `audio_codes_B`, `mask` (Optional).
- **Options**:
  - `mode`: (`blend`, `lerp`, `inject`, `difference_injection`, `replace`, `concatenate`, etc.)
  - `scale_mode`: Handle sequence length mismatches (`scale_B_to_A`, `loop_match`, `pad_to_match`).
  - `alpha`, `ratio`, `weight`: Tuning variables for the respective blend modes.
- **Outputs**: `audio_codes` (`LIST`).

### 6. AceStepAudioCodesUnaryOp

*File: `nodes/audio_codes_unary_op_node.py`*

Transforms a *single* list of audio codes using masks and mathematical operations. Unique among nodes, it outputs a diagnostic visual representation of the tokens.

- **Inputs**: `audio_codes`, `mask` (Optional).
- **Options**:
  - `mode`: (`noop_visualize`, `gate`, `scale_masked`, `noise_masked`, `fade_out`).
  - `visualization_type`: Generates diagnostic images (`ssm`, `pca_ribbon`, `music_radar`, `song_texture`, `spiral`).
  - `length_pct`, `strength`, `sigma`.
- **Outputs**: `audio_codes` (`LIST`), `visualization` (`IMAGE`).

### 7. AceStepAudioCodesToSemanticHints

*File: `nodes/audio_codes_to_semantic_hints_node.py`*

Converts low-level 5Hz audio codes up to 25Hz semantic hints for specific VAE architectural requirements.

- **Inputs**: `audio_codes` (`LIST`).
- **Outputs**: `semantic_hints` (`TENSOR`).

### 8. AceStepSemanticHintsToAudioCodes

*File: `nodes/semantic_hints_to_audio_codes_node.py`*

Downsamples 25Hz semantic hint tensors back to 5Hz explicit audio code token IDs.

- **Inputs**: `semantic_hints` (`TENSOR`).
- **Outputs**: `audio_codes` (`LIST`).

---

## Part 3: Tensor Math (Continuous Tensors)

### 9. AceStepTensorMixer

*File: `nodes/tensor_mixer_node.py`*

The continuous tensor equivalent to the Audio Codes Mixer. Mixes `lyrics_tensor` or `timbre_tensor` inputs using spatial or scalar masking.

- **Inputs**: `tensor_A`, `tensor_B`, `mask` (Optional).
- **Options**: `mode` (`blend`, `lerp`, etc.), scaling, and weighting.
- **Outputs**: `mixed_tensor` (`TENSOR`).

### 10. AceStepTensorUnaryOp

*File: `nodes/tensor_unary_op_node.py`*

Single-tensor operations (zeroing, noising, fading) applied directly to conditioning embedding space.

- **Inputs**: `tensor`, `mask` (Optional).
- **Outputs**: `tensor` (`TENSOR`).

---

## Part 4: Mask Generation

### 11. AceStepAudioMask

*File: `nodes/audio_mask_node.py`*

Generates ComfyUI `MASK` objects specifically structured for time-series audio data, allowing for precise temporal gating.

- **Inputs**: None required.
- **Outputs**: `MASK`.

### 12. AceStepTensorMaskGenerator

*File: `nodes/tensor_mask_node.py`*

A primitive masking toolbox creating fractions, hard ranges, or smoothed window functions over lengths.

- **Options**: `mask_type`, `length`, `fade_in/out`.
- **Outputs**: `MASK`.

---

## Part 5: Component Loaders

*A collection of nodes designed to ingest saved tensors directly from the disk. Found bridging standard checkpoint loaders into Scromfy modularity.*

1. **AceStepAudioCodesLoader** (`load_audio_codes_node.py`): Loads `.json` or `.safetensors` lists of audio code IDs.
    - **Options**: `codes_file` (Dropdown), `none_action` (What to do if file fails).
    - **Outputs**: `audio_codes` (`LIST`).
2. **AceStepLyricsTensorLoader** (`load_lyrics_tensor_node.py`): Loads compiled 1024-dim vocal guidance tensors.
    - **Options**: `tensor_file` (Dropdown), `none_action`.
    - **Outputs**: `lyrics_tensor` (`TENSOR`).
3. **AceStepTimbreTensorLoader** (`load_timbre_tensor_node.py`): Loads compiled background/timbre guidance tensors.
    - **Options**: `tensor_file` (Dropdown), `none_action`.
    - **Outputs**: `timbre_tensor` (`TENSOR`).
4. **AceStepConditioningLoad** (`load_conditioning_node.py`): Helper to load full metadata bundles.
    - **Options**: `mixed_file` (Dropdown prefix).
    - **Outputs**: `conditioning` (`CONDITIONING`).
5. **AceStepConditioningMixerLoader** (`load_mixed_conditioning_node.py`): Advanced loader capable of ingesting multiple components and pre-mixing them according to predefined ratios.
    - **Options**: `timbre_source`, `lyrics_source`, `codes_source` (Dropdowns), and `none_action`s.
    - **Outputs**: `conditioning` (`CONDITIONING`).

---

## Part 6: Introspection & Tools

### 18. AceStepConditioningExplore

*File: `nodes/conditioning_view_node.py`*
A diagnostic tool that safely prints shape data, dimension layouts, and metadata keys inside a conditioning object directly to the terminal for debugging graph connections.

- **Inputs**: `text_cond` (`CONDITIONING`).
- **Outputs**: `json_text` (`STRING`).

### 19. AceStepAudioCodesUnderstand

*File: `nodes/audio_codes_decode_node.py`*
Reverses the initial LM logic, parsing existing 5Hz token IDs back into readable lyrics hints or generated musical tags if the LLM encoded them linearly.

- **Inputs**: `llm` (`ACE_LLM`), `audio_codes` (`LIST`).
- **Options**: `temperature`, `top_k`, `top_p`, `max_new_tokens`.
- **Outputs**: `full_output` (`STRING`), `lyrics` (`STRING`), `metadata` (`DICT`).

### 20. AceStepConditioningInspector

*File: `nodes/conditioning_inspector_node.py`*

A more advanced version of the standard view node that provides a detailed, formatted dump of all tensors and metadata keys within an ACE-Step conditioning object.

- **Inputs**: `conditioning` (`CONDITIONING`), `label` (`STRING`).
- **Outputs**: `conditioning` (`CONDITIONING`) (Passthrough).

---

## Part 7: Persistence

A small suite of nodes dedicated to bridging generation sessions by saving mathematically intact conditioning bundles and raw tensors to the disk.

### 20. AceStepConditioningSave

*File: `nodes/save_conditioning_node.py`*

Takes a full `CONDITIONING` object and safely serializes all internal tensors (`timbre`, `lyrics`) and the 5Hz `audio_codes` lists into a single bundled `.safetensors` / `.json` pair on disk. This allows for instant retrieval in future workflows.

- **Inputs**: `conditioning` (`CONDITIONING`).
- **Outputs**: None.

### 21. AceStepTensorSave

*File: `nodes/save_tensor_node.py`*

Saves an isolated raw tensor object to `.safetensors` format. Useful when intercepting masks or continuous data specifically.

- **Inputs**: `tensor` (`TENSOR`).
- **Outputs**: None.

---

---

## Part 9: Base Model Tasks

These nodes prepare conditioning specifically for the **ACE-Step 1.5 Base** (non-turbo) model to perform specialized audio tasks like stem extraction and contextual generation.

### 25. AceStepBaseExtract

*File: `nodes/conditioning_base_extract_node.py`*

Prepares conditioning to extract a specific solo track from a mixed audio source.

- **Inputs**:
  - `conditioning` (`CONDITIONING`): The base conditioning bundle.
  - `track_name`: The specific instrument/track to extract (e.g., `vocals`, `drums`).
- **Outputs**: `conditioning` (`CONDITIONING`).

### 26. AceStepBaseLego

*File: `nodes/conditioning_base_lego_node.py`*

Prepares conditioning for "lego" style generation, where a new track is generated to fit the musical context of existing audio.

- **Inputs**:
  - `conditioning` (`CONDITIONING`): The base conditioning bundle.
  - `latent_image` (`LATENT`): The contextual audio latents to build upon.
  - `track_name`: The instrument/track to generate.
- **Optional**:
  - `region_start_s` / `region_end_s`: Define a specific time window for generation (0 for full).
- **Outputs**:
  - `conditioning` (`CONDITIONING`)
  - `latent_image` (`LATENT`)
  - `mask` (`MASK`): A temporal mask for the sampler.

### 27. AceStepBaseComplete

*File: `nodes/conditioning_base_complete_node.py`*

Prepares conditioning for multi-track accompaniment completion. It automatically injects instructions to add several missing instruments at once.

- **Inputs**: `conditioning` (`CONDITIONING`).
- **Options**: Boolean flags for every supported track class (drums, bass, vocals, etc.).
- **Outputs**: `conditioning` (`CONDITIONING`).
